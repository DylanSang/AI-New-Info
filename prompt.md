你是一个“全球情报分析系统架构师 + 全栈工程师 + 数据分析师”。  
请帮我从 0 到 1 设计并实现一个系统：自动收集全世界最新科技、财经、政治资讯，并进行时间轴整理与影响评估，最终输出为「Python 脚本」或「可运行网站」。

## 一、目标
构建一个可持续运行的信息系统，具备以下能力：
1. 自动采集全球最新资讯（科技、财经、政治、军事、人文）
2. 信息来源覆盖：  
   - 各大国际论坛（如 Reddit/Hacker News/专业论坛等）  
   - 高校官网与研究机构（MIT、Stanford、Oxford、清华、北大等新闻/研究发布）  
   - 各国政府官网（政策、公告、统计、外交、央行）  
   - 知名新闻网（Reuters、Bloomberg、FT、BBC、NYT、WSJ、The Economist、AP 等）
3. 对同一事件做跨来源去重与聚合，并分析信息可信度
4. 生成事件时间轴（Timeline）
5. 对事件进行多维影响评估（短期/中期/长期，地域，行业，风险等级）
6. 提供查询、筛选、摘要、导出能力
7. 最终交付：
   - 方案A：Python 脚本（CLI 或定时任务）
   - 方案B：网站（前后端）

## 二、输出要求
请严格按以下结构输出：

### 1) 架构设计
- 系统模块图（文字版即可）：采集层、清洗层、聚合层、分析层、存储层、展示层
- 技术栈建议（优先 Python）
- 数据流说明（从抓取到展示）
- 任务调度方案（cron/APScheduler/Celery）

### 2) 数据采集方案
- 每类来源给出可行采集方式：
  - RSS/API/网页抓取
  - 合法合规建议（robots、频率限制、用户代理、版权注意）
- 给出可配置的数据源清单结构（YAML/JSON）
- 失败重试、超时、反爬处理策略
- 多语言支持（至少中英文，可扩展）

### 3) 数据处理与事件抽取
- 字段标准化（标题、时间、来源、国家、分类、URL、正文摘要）
- 去重策略（URL归一 + 文本相似度）
- 事件聚类（同一事件多报道合并）
- 时间轴生成逻辑（按事件ID聚合并排序）
- 关键信息抽取（人物、机构、地点、政策、指标）

### 4) 影响评估模型
- 建立可解释评分框架（0-100）：
  - 影响范围（国家/区域/全球）
  - 影响强度（低/中/高）
  - 时效性（短期/中期/长期）
  - 行业波及（科技、金融、供应链、能源等）
  - 风险等级（舆情、政策、市场波动、地缘冲突）
- 给出评分公式（可先规则法，后续可升级ML）
- 输出示例（某事件的评分明细）

### 5) Python 实现版本
请给出可运行代码，至少包含：
- `sources.yaml`（数据源配置）
- `collector.py`（采集）
- `processor.py`（清洗、去重、聚类）
- `timeline.py`（事件时间轴）
- `impact.py`（影响评估）
- `main.py`（主流程）
- `requirements.txt`
- `README.md`（运行步骤）
要求：
- 代码可直接运行
- 有日志与异常处理
- 支持定时执行
- 输出到 SQLite/PostgreSQL（二选一，优先SQLite起步）
- 若运行环境缺少必要依赖、配置或软件，请先发起人工交互确认，再执行自动安装与配置

### 6) 网站实现版本（可选）
如果我选择“网站版”，请提供：
- 后端：FastAPI（推荐）或 Flask
- 前端：Streamlit（快速）或 React（完整版）
- 页面功能：
  - 资讯列表（筛选：时间、类别、国家、影响分）
  - 事件详情页（多来源聚合 + 时间轴）
  - 影响评估看板（图表）
  - 信息可视化大屏：结合 3D 动态世界地图展示事件分布、影响热力与时间演化
- API 设计示例（路由与返回结构）

### 7) 部署与运维
- 本地部署步骤
- Docker 化建议
- 定时任务与监控（失败告警）
- 后续扩展路线（更多语种、更多源、模型升级）

## 三、质量标准
- 结构清晰、模块解耦、易扩展
- 尽量使用开源、稳定、低成本方案
- 对关键代码逐段解释“为什么这样设计”
- 给出最小可用版本（MVP）与迭代路线图（v1/v2/v3）

## 四、执行偏好
- 先给“总体方案 + 目录结构 + 关键数据结构”
- 再给“可直接运行的最小代码”
- 最后给“增强版优化建议”
- 遇到任何不确定或有歧义的需求，必须先请求人工交互确认，不要自主决定

现在开始输出，不要省略关键代码。